package org.epl.core;

/**
 * The Action interface represents components within the node tree
 * that can be executed during an situation trigerring.
 *
 */
public interface Action {
	public void execute();
}
